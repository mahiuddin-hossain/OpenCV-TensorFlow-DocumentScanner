<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FF1A1A1A">

    <!-- Scanned Image -->
    <ImageView
        android:id="@+id/scanned_image"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:scaleType="fitCenter"
        android:padding="8dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@id/filter_container"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- ★ Loading ProgressBar (মাঝখানে থাকবে, ডিফল্টভাবে লুকানো থাকবে) -->
    <ProgressBar
        android:id="@+id/progress_bar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="gone"
        android:elevation="10dp"
        app:layout_constraintTop_toTopOf="@id/scanned_image"
        app:layout_constraintBottom_toBottomOf="@id/scanned_image"
        app:layout_constraintStart_toStartOf="@id/scanned_image"
        app:layout_constraintEnd_toEndOf="@id/scanned_image" />

    <!-- Filter Chips -->
    <HorizontalScrollView
        android:id="@+id/filter_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:scrollbars="none"
        android:paddingBottom="8dp"
        app:layout_constraintBottom_toTopOf="@id/bottom_bar">

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:paddingHorizontal="12dp"
            android:gravity="center">

            <TextView
                android:id="@+id/filter_original"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Original"
                android:textColor="@android:color/white"
                android:textSize="13sp"
                android:background="@drawable/filter_chip_bg"
                android:paddingHorizontal="20dp"
                android:paddingVertical="10dp"
                android:layout_marginHorizontal="4dp" />

            <TextView
                android:id="@+id/filter_magic"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="✨ Magic"
                android:textColor="@android:color/white"
                android:textSize="13sp"
                android:background="@drawable/filter_chip_bg"
                android:paddingHorizontal="20dp"
                android:paddingVertical="10dp"
                android:layout_marginHorizontal="4dp" />

            <TextView
                android:id="@+id/filter_bw"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="B&amp;W"
                android:textColor="@android:color/white"
                android:textSize="13sp"
                android:background="@drawable/filter_chip_bg"
                android:paddingHorizontal="20dp"
                android:paddingVertical="10dp"
                android:layout_marginHorizontal="4dp" />

            <TextView
                android:id="@+id/filter_grayscale"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Grayscale"
                android:textColor="@android:color/white"
                android:textSize="13sp"
                android:background="@drawable/filter_chip_bg"
                android:paddingHorizontal="20dp"
                android:paddingVertical="10dp"
                android:layout_marginHorizontal="4dp" />

        </LinearLayout>
    </HorizontalScrollView>

    <!-- Bottom Action Bar -->
    <LinearLayout
        android:id="@+id/bottom_bar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="16dp"
        android:gravity="center"
        app:layout_constraintBottom_toBottomOf="parent">

        <Button
            android:id="@+id/btn_retake"
            android:layout_width="0dp"
            android:layout_height="48dp"
            android:layout_weight="1"
            android:layout_marginEnd="8dp"
            android:text="Retake"
            android:textColor="@android:color/white"
            android:backgroundTint="#444444"
            android:textAllCaps="false" />

        <Button
            android:id="@+id/btn_save"
            android:layout_width="0dp"
            android:layout_height="48dp"
            android:layout_weight="1"
            android:layout_marginHorizontal="8dp"
            android:text="Save"
            android:textColor="@android:color/white"
            android:backgroundTint="#2196F3"
            android:textAllCaps="false" />

        <Button
            android:id="@+id/btn_share"
            android:layout_width="0dp"
            android:layout_height="48dp"
            android:layout_weight="1"
            android:layout_marginStart="8dp"
            android:text="Share"
            android:textColor="@android:color/white"
            android:backgroundTint="#444444"
            android:textAllCaps="false" />

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>